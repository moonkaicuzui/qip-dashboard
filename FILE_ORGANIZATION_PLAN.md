# 📁 프로젝트 파일 정리 계획

## 🎯 목표
- 중복 파일 제거
- 논리적인 디렉토리 구조 구성
- 파일 접근성 개선
- 유지보수 용이성 향상

## 📊 현재 상황 분석

### 중복/불필요 파일
1. **5PRS 관련 중복 파일들**
   - `5prs.sh`, `5prs_embedded.sh`, `5prs_standalone.sh` → 통합 필요
   - `create_embedded_5prs.py`, `create_standalone_5prs.py` → 제거
   - `5PRS_DASHBOARD_EMBEDDED/` → 제거 (원본만 유지)
   - `5PRS_Dashboard_Standalone.html` → 제거

2. **테스트/임시 파일들**
   - `test_5prs_api.py` → tests/ 폴더로 이동
   - `test_final.sh` → tests/ 폴더로 이동
   - `.playwright-mcp/` → 임시 폴더, 정리 필요

3. **문서 파일들 (분산됨)**
   - 각종 `.md` 파일들 → docs/ 폴더로 통합

## 🏗️ 제안하는 디렉토리 구조

```
대시보드 인센티브 테스트11/
│
├── 📂 src/                      # 핵심 소스 코드
│   ├── core/                    # 핵심 비즈니스 로직
│   │   ├── step0_create_monthly_config.py
│   │   ├── step1_인센티브_계산_개선버전.py
│   │   └── step2_dashboard_version4.py
│   │
│   ├── api/                     # API 서버 관련
│   │   ├── 5prs_data_api.py
│   │   └── google_drive_manager.py
│   │
│   ├── integration/             # 데이터 통합
│   │   ├── integrate_5prs_data.py
│   │   ├── auto_run_with_drive.py
│   │   └── sync_previous_incentive.py
│   │
│   ├── dashboards/              # 대시보드 생성
│   │   ├── integrated_dashboard_final.py
│   │   └── generate_management_dashboard_v6_enhanced.py
│   │
│   └── utils/                   # 유틸리티
│       ├── convert_attendance_data.py
│       ├── validate_hr_data.py
│       └── verify_all_positions.py
│
├── 📂 5PRS DASHBOARD/           # 5PRS 대시보드 UI (유지)
│   ├── index.html
│   ├── style.css
│   └── js/
│
├── 📂 config_files/             # 설정 파일 (유지)
│   ├── position_condition_matrix.json
│   └── config_*.json
│
├── 📂 input_files/              # 입력 데이터 (유지)
│   ├── attendance/
│   ├── AQL history/
│   ├── 5prs/
│   └── *.csv
│
├── 📂 output_files/             # 출력 파일 (유지)
│   ├── dashboards/
│   │   └── 5prs/
│   │       └── data/
│   ├── *.html
│   ├── *.xlsx
│   └── *.csv
│
├── 📂 logs/                     # 로그 파일
│   ├── 5prs_api.log
│   ├── drive_sync.log
│   └── *.log
│
├── 📂 tests/                    # 테스트 스크립트
│   ├── test_5prs_api.py
│   ├── test_final.sh
│   └── validate_dashboard.py
│
├── 📂 docs/                     # 문서
│   ├── README.md
│   ├── CLAUDE.md
│   ├── SYSTEM_ARCHITECTURE_V2.md
│   ├── DEPENDENCY_MAP.md
│   ├── UI_UX_IMPLEMENTATION_REPORT.md
│   ├── GOOGLE_DRIVE_SETUP.md
│   └── FILE_ORGANIZATION_PLAN.md
│
├── 📂 scripts/                  # 실행 스크립트
│   ├── action.sh               # 메인 실행 스크립트
│   └── 5prs.sh                 # 5PRS 전용 스크립트
│
├── 📂 credentials/              # 인증 정보 (유지)
│   └── service-account-key.json
│
├── 📂 .cache/                   # 캐시 (유지)
│   └── drive_sync/
│
└── 📂 error_review/             # 에러 리뷰 (유지)
    └── *.xlsx
```

## 🔄 정리 작업 계획

### Phase 1: 백업
1. 전체 프로젝트 백업 생성
2. Git commit으로 현재 상태 저장

### Phase 2: 중복 파일 제거
1. 5PRS 관련 중복 파일 제거
2. 테스트 파일 이동
3. 임시 파일 정리

### Phase 3: 디렉토리 재구성
1. src/ 하위 폴더 생성
2. 파일들을 적절한 위치로 이동
3. import 경로 업데이트

### Phase 4: 스크립트 업데이트
1. action.sh 경로 업데이트
2. 각 Python 스크립트의 import 경로 수정

### Phase 5: 검증
1. action.sh 실행 테스트
2. 각 대시보드 동작 확인
3. API 서버 실행 확인

## ⚠️ 주의 사항
- 데이터 손실 방지를 위한 백업 필수
- 실행 중인 프로세스 종료 후 작업
- 경로 변경 시 모든 참조 업데이트 필요

## 📝 예상 효과
- 프로젝트 구조 명확성 향상
- 유지보수 용이성 증가
- 중복 코드 제거로 용량 절감
- 팀 협업 효율성 향상