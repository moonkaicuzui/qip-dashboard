name: Deploy Dashboard to GitHub Pages

on:
  push:
    branches: [ main, master ]
  schedule:
    # 매월 1일 오전 9시 자동 배포 (KST)
    - cron: '0 0 1 * *'
  workflow_dispatch: # 수동 실행 가능

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy openpyxl beautifulsoup4

    - name: Generate Dashboard
      run: |
        # 현재 월 기준으로 대시보드 생성
        MONTH=$(date +%-m)
        YEAR=$(date +%Y)
        python integrated_dashboard_final.py --month $MONTH --year $YEAR || echo "Dashboard generation skipped"

    - name: Prepare GitHub Pages
      run: |
        # docs 폴더 생성 (GitHub Pages용)
        mkdir -p docs

        # 최신 대시보드 복사
        if ls output_files/Incentive_Dashboard_*.html 1> /dev/null 2>&1; then
          cp output_files/Incentive_Dashboard_*.html docs/
          # 가장 최신 파일을 index.html로 복사
          latest_dashboard=$(ls -t output_files/Incentive_Dashboard_*.html | head -n 1)
          cp "$latest_dashboard" docs/index.html
        fi

        # 모바일 최적화된 인덱스 페이지 생성
        cat > docs/index.html << 'HTML'
        <!DOCTYPE html>
        <html lang="ko">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
            <title>QIP 인센티브 대시보드</title>
            <style>
                * {
                    margin: 0;
                    padding: 0;
                    box-sizing: border-box;
                }
                body {
                    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    min-height: 100vh;
                    padding: 20px;
                }
                .container {
                    max-width: 800px;
                    margin: 0 auto;
                }
                .header {
                    background: rgba(255, 255, 255, 0.95);
                    border-radius: 20px;
                    padding: 30px;
                    margin-bottom: 20px;
                    text-align: center;
                    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
                }
                h1 {
                    color: #333;
                    margin-bottom: 10px;
                    font-size: 24px;
                }
                .subtitle {
                    color: #666;
                    margin-bottom: 20px;
                }
                .dashboard-list {
                    background: white;
                    border-radius: 15px;
                    padding: 20px;
                    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
                }
                .dashboard-item {
                    display: block;
                    padding: 15px 20px;
                    margin: 10px 0;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    text-decoration: none;
                    border-radius: 10px;
                    transition: transform 0.3s, box-shadow 0.3s;
                    text-align: center;
                    font-weight: bold;
                }
                .dashboard-item:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
                }
                .info {
                    background: #f8f9fa;
                    border-left: 4px solid #667eea;
                    padding: 15px;
                    margin: 20px 0;
                    border-radius: 5px;
                }
                @media (max-width: 768px) {
                    body {
                        padding: 10px;
                    }
                    h1 {
                        font-size: 20px;
                    }
                    .header {
                        padding: 20px;
                    }
                }
            </style>
        </head>
        <body>
            <div class="container">
                <div class="header">
                    <h1>📊 QIP 인센티브 대시보드</h1>
                    <p class="subtitle">Quality Inspection Process Incentive Dashboard</p>
                </div>

                <div class="dashboard-list">
                    <h2 style="text-align: center; margin-bottom: 20px; color: #333;">
                        📱 모바일 최적화 대시보드
                    </h2>

                    <div class="info">
                        <strong>ℹ️ 안내</strong><br>
                        아래 버튼을 클릭하여 대시보드를 확인하세요.<br>
                        모바일과 PC 모두에서 최적화되어 표시됩니다.
                    </div>

                    <a href="#" class="dashboard-item" id="latest-dashboard">
                        최신 대시보드 보기
                    </a>
                </div>

                <div class="info" style="margin-top: 30px; text-align: center;">
                    <small>
                        🔄 매월 자동 업데이트<br>
                        📱 모바일 반응형 지원<br>
                        🌐 다국어 지원 (한국어/English/Tiếng Việt)
                    </small>
                </div>
            </div>

            <script>
                // 최신 대시보드 파일 찾기
                const dashboards = [];
                // GitHub Actions에서 생성된 파일 목록 삽입

                // 가장 최신 대시보드로 링크 설정
                const latestLink = document.getElementById('latest-dashboard');
                if (dashboards.length > 0) {
                    latestLink.href = dashboards[0];
                }
            </script>
        </body>
        </html>
HTML

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v2
      with:
        path: ./docs

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v2