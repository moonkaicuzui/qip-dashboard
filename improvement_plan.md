# 인센티브 대시보드 파일 경로 문제 개선 계획

## 🎯 목표
action.sh 실행 시 입력한 월/연도에 해당하는 파일을 정확히 찾고 처리하도록 개선

## 🔍 현재 상황 분석

### 1. 파일 누락 현황
- ❌ **attendance data august_converted.csv** - converted 폴더에 없음
  - 원본 파일: `input_files/attendance/original/attendance data august.csv` (존재함)
  - 변환된 파일: `input_files/attendance/converted/attendance data august_converted.csv` (없음)

- ❌ **2025년 7월 인센티브 지급 세부 정보.csv** - 파일 자체가 없음
  - 현재 있는 파일: `2025년 8월 인센티브 지급 세부 정보.csv`
  - 8월 계산을 위해서는 7월 데이터가 필요함

### 2. 설정 파일 문제점
`config_files/config_august_2025.json`:
- 잘못된 경로들이 하드코딩되어 있음
- 파일 존재 여부 검증 없이 경로만 지정

## 🛠️ 개선 방안

### 단계 1: 즉시 적용 가능한 해결책

#### 1.1 출근 데이터 변환 자동화
```python
# step1 실행 전 출근 데이터 자동 변환
def convert_attendance_if_needed(month, year):
    original = f"input_files/attendance/original/attendance data {month}.csv"
    converted = f"input_files/attendance/converted/attendance data {month}_converted.csv"
    
    if os.path.exists(original) and not os.path.exists(converted):
        # 변환 로직 실행
        convert_attendance(original, converted)
```

#### 1.2 Config 파일 생성 시 파일 검증
- `step0_create_monthly_config.py` 수정
- 실제 존재하는 파일만 config에 포함
- 없는 파일은 warning과 함께 null로 설정

### 단계 2: 시스템 개선

#### 2.1 파일 자동 탐색 기능
```python
def find_matching_files(pattern, directory):
    """패턴에 맞는 파일 자동 탐색"""
    # fuzzy matching으로 유사한 파일명 찾기
    # 월/연도 정보로 가장 적합한 파일 선택
```

#### 2.2 오류 메시지 개선
- 어떤 파일이 없는지 구체적으로 표시 ✅ (완료)
- 파일을 어디서 가져와야 하는지 안내 추가
- Google Drive 동기화 필요한 파일 목록 제공

#### 2.3 데이터 검증 레이어 추가
```python
class DataValidator:
    def validate_all_required_files(self, config):
        missing_files = []
        for key, path in config['file_paths'].items():
            if not os.path.exists(path):
                missing_files.append((key, path))
        return missing_files
```

## 📝 즉시 수정 필요한 파일들

1. **config_files/config_august_2025.json**
   - 실제 존재하는 파일 경로로 수정
   
2. **src/step0_create_monthly_config.py**
   - 파일 존재 여부 검증 로직 추가
   - 자동 경로 탐색 기능 추가

3. **src/step1_인센티브_계산_개선버전.py**
   - 출근 데이터 자동 변환 기능 추가
   - 누락된 파일에 대한 명확한 에러 메시지

## 🚀 실행 순서

1. **긴급 수정** (지금 바로)
   - 출근 데이터 변환 스크립트 생성
   - Config 파일 경로 수정

2. **시스템 개선** (다음 단계)
   - 파일 자동 탐색 기능 구현
   - 데이터 검증 레이어 추가

3. **문서화** (완료 후)
   - 필요한 파일 목록 문서화
   - Google Drive 동기화 가이드 작성

## ⚠️ 주의사항

**CLAUDE.md 원칙 준수:**
- 가짜 데이터 생성 금지 ❌
- 실제 데이터가 없으면 오류 메시지 표시 ✅
- 시스템은 실제 데이터만 사용 ✅

## 📌 다음 액션

1. 출근 데이터 변환 처리
2. 7월 인센티브 데이터 확인 (사용자에게 요청)
3. Config 파일 자동 수정 기능 구현